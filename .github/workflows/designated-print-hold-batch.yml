name: Designated print hold batch

on:
  push:
    branches:
      - preview/schedule-ux-20260903
    paths:
      - '.github/workflows/designated-print-hold-batch.yml'
      - '.github/scripts/apply-designated-print-hold.py'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply confirmed parent-spec print hold
        run: python .github/scripts/apply-designated-print-hold.py
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Targeted and related regression
        run: |
          node scripts/designated-print-hold-regression.mjs
          npm run test:record-types
          npm run test:workshop-record-types
          npm run test:security
          npm run test:app-core-safety
      - name: Full build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: test-public-key
        run: npx next build
      - name: Restore generated build artifacts
        run: |
          git checkout -- next-env.d.ts tsconfig.json public/tesseract || true
      - name: Commit verified source changes only
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          rm -f .github/scripts/apply-designated-print-hold.py .github/workflows/designated-print-hold-batch.yml
          git add app/inspection/page.tsx scripts/designated-print-hold-regression.mjs .github/scripts/apply-designated-print-hold.py .github/workflows/designated-print-hold-batch.yml
          git diff --cached --quiet && exit 0
          git commit -m 'fix: hold designated record formal printing'
          git push origin HEAD:preview/schedule-ux-20260903
